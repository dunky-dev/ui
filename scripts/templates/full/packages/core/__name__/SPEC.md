# __name__ — spec

## Intent

<!-- The problem this primitive solves and the behavior it guarantees, in prose.
State what it is meant to do, not a field checklist. Trace behavior to prior art
where it exists (a source library, a bug report, a test in this repo). -->

TODO: describe the __name__ interaction and why it exists.

## The interaction

<!-- The lifecycle: the states, the gestures that move between them, and the
terminal outcomes. An ASCII state diagram helps — plain | - + only. -->

TODO: describe the states and transitions.

## What the consumer observes

<!-- The callback sequences per real scenario. Order is API, not an
implementation detail — spell it out per scenario, then state the guarantees
once. -->

TODO: table of scenario -> observed callbacks, plus guarantees.

## Driver obligations

<!-- What a conforming substrate driver must translate and honor. `src/types.ts`
is the typed source of truth for the machine's event vocabulary. -->

TODO: list the driver contract.

## Positions

Substrate-independent design decisions, with their standing. These rows are
never deleted — a change of heart flips the status with reasoning.
Statuses: `adopted` (differs from a source, on purpose) · `deferred`
(consciously not built; says what breaks) · `rejected` (considered and
refused; says why).

| Position | Why  | Status |
| -------- | ---- | ------ |
| TODO     | TODO | TODO   |
