{
  "extends": "expo/tsconfig.base",
  "compilerOptions": {},
  // Claims only the Expo app shell — dialog/** stays on the root tsconfig
  // (with its workspace paths); an unscoped nearest-tsconfig would shadow it.
  "include": ["index.ts", ".rnstorybook"]
}
