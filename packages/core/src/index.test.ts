import { expect, test } from 'vitest'
import { version } from './index'

// Placeholder test so the suite (and CI) is green on a fresh scaffold.
// Replace with real tests for this repo's public API.
test('exposes a version', () => {
  expect(version).toBe('0.0.0')
})
