---
---

Add `SPEC.md` to the eight util and hook packages that lacked one — the two
core utils (`controllable`, `overlay`), the four DOM utils (`focus-trap`,
`navigation`, `overlay`, `scroll-lock`), and the two React hooks
(`use-focus-trap`, `use-scroll-lock`). Docs only; no package changes, so this
changeset carries no version bump.
